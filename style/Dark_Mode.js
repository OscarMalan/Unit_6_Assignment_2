/**
 * This function allows the button for dark mode to work, while some of the other js code that there will be wont be necesarry on all pages
 */
function Dark_Mode_Button() {
    let element = document.body;
    element.classList.toggle("Dark_Mode");
 }